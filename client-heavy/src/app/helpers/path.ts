export const createMovePath = (x: number, y: number): string => {
    return 'M' + x + ' ' + y + ' ';
};
export const createLinePath = (x: number, y: number): string => {
    return 'L' + x + ' ' + y + ' ';
};
